import React, { FC } from "react";
import styled from "styled-components";

const FeaturesPage: FC = () => {
  return <Wrapper>FeaturesPage</Wrapper>;
};

const Wrapper = styled.div``;

export default FeaturesPage;
