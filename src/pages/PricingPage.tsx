import React, { FC } from "react";
import styled from "styled-components";

const PricingPage: FC = () => {
  return <Wrapper>PricingPage</Wrapper>;
};

const Wrapper = styled.div``;

export default PricingPage;
