import React, { FC } from "react";
import styled from "styled-components";

const DemosPage: FC = () => {
  return <Wrapper>DemosPage</Wrapper>;
};

const Wrapper = styled.div``;

export default DemosPage;
