import React, { FC } from "react";
import styled from "styled-components";

const Footer: FC = () => {
  return <Wrapper>f</Wrapper>;
};

const Wrapper = styled.footer`
  padding: 2rem;
`;

export default Footer;
